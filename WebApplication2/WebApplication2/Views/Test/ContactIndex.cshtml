@model IEnumerable<WebApplication2.Models.Contact>

@{
    ViewBag.Title = "ContactIndex";
}

<h2>ContactIndex</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContactNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PersonId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContactNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PersonId)
        </td>
        <td>
            @Html.ActionLink("Edit", "ContactEdit", new { id=item.ContactId }) |
            @Html.ActionLink("Details", "ContactDetails", new { id=item.ContactId }) |
            @Html.ActionLink("Delete", "ContactDelete", new { id=item.ContactId })
        </td>
    </tr>
}

</table>
